= PayuRails

This project rocks and uses MIT-LICENSE.

== Usage

  rails g payu_rails:install

Add line to one of your main models:
  has_many :commissions,
           :class_name => "PayuRails::Commission",
           :as => :entity

== Helper methods
  = payu_link(payment_pay_path(@payment), :class => "pull-center", :method => :post)
